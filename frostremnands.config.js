module.exports = {
  /*
   *  offline = Offline First
   *  online = Hability general requests
   */
  target: "offline",
  /*
   *  false = disabled window chromium reload
   *  true = window chromium reload with Ctrc+R
   */
  reload: false,
  /*
   *  false = disabled dev console
   *  true = console in dev mode
   */
  console: true,
  /*
   *  actually website redirect
   */
  website: "https://novout.github.io/frost-remnands/"
}